<!-- https://www.google.com/ping?sitemap=https://good-weed.com/sitemap -->
<% SELECT_CITIES_LIST.each_with_index do |city, idx| %>
<%# next unless idx == 0 %>
<%=$root_url%>/delivery/<%=city%>

<% end %>
<% $users.all.each_with_index do |user, idx| %>
<%# next unless idx == 0 %>
<%=$root_url%><%=user_link(user)%>

<% end %>